<ul>
  <% @main_users.each do |user| %>
    <li><%= user.surname %> <%= user.name %> (<%= link_to user.upn || "no mail", dm_unibo_common.impersonate_path(user) %>)</li>
  <% end %>
</ul>

<ul>
  <% @users.each do |user| %>
    <li><%= user.surname %> <%= user.name %> (<%= link_to user.upn || "no mail", dm_unibo_common.impersonate_path(user) %>)</li>
  <% end %>
</ul>

